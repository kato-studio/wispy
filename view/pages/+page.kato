#include(c/Parent as Parent)
#include(c/Header as Header)
#include(c/Footer as Footer)

<script>
  console.log('hello from page')
</script>

<#Parent(links:{{.links}}) class="hello-world-header" />

<h1>Home page</h1>
<#if(wecomeMessage)>
  <h2>{{.welcomeMessage}}</h2>
</#if>

<#Parent(links:{{.links}}, company:{{.company}})>
  <ul>
    <#for(item in .links)>
      <li>{{.item}}</li>
    </#for>
  </ul>
</#Parent>

<#Footer() foo="bar" />