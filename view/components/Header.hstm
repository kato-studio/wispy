<div class="flex">
  <h2>Header</h2>
  <div>
    <_render each="link" in={{links}}>
      <a href="{{link.url}}">{{link.text}}</a>
    </_render>

    <_render each="link" in={{links}}>
      <a href="{{link.url}}">{{link.text}}</a>
    </_render>
  </div>
</div>